spring:
  datasource:
    url: jdbc:h2:mem:salgados_lucia;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop #ele cria as tabelas na subida e derruba na descida.
    database-platform: org.hibernate.dialect.H2Dialect